bindAddr = ":8080"
readTimeout = 10
writeTimeout= 10

[logger]
level = "debug"
encoding = "json"
outputPaths = ["../access_log"]
errorOutputPaths = ["../error_log"]

[logger.encoderConfig]
messageKey = "msg"
timeKey = "finished"
levelKey = "lvl"
encodeLevel = "lowercase"
encodeTime = "iso8601"